# Copy to deploy.env on the production host (Lightsail) and fill in.
# deploy.env is gitignored - it holds only AWS resource IDs, no secrets.
S3_BUCKET=your-frontend-bucket
CLOUDFRONT_DISTRIBUTION_ID=E1ABCDEF23GHIJ
AWS_REGION=us-west-2
# AWS_PROFILE=default
# PM2_APP_NAME=stowaway-api
# STOWAWAY_ENV_FILE=/etc/stowaway.env
