# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
.npm-cache
*.local
.env

# schema.local.prisma (SQLite) migration history is per-developer and locked to
# the "sqlite" provider — it must never be committed alongside the production
# postgresql schema.prisma, which shares this same prisma/ folder.
server/prisma/migrations/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.db
docs/SETUP.md
